services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: workflow_platform
      POSTGRES_USER: workflow_platform
      POSTGRES_PASSWORD: workflow_platform
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
